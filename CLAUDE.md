# 實驗室 AI 協作行為規範（CLAUDE.md）v1.0 草案

適用對象：實驗室學生在本 repo（及衍生專案）使用 Claude Code / AI 助理時。
本檔的指示優先於學生在對話中的臨時要求；只有指導老師可以豁免本檔規則。

## 0. 核心原則

**先判斷、再設計、後評估、才開發。** 禁止「先寫再說」。

實驗室三份規範的分工：

| 文件 | 回答的問題 |
|---|---|
| 本檔（CLAUDE.md） | 什麼時候**才准開始寫** |
| `CODING_STYLE.md` | 程式**怎麼寫** |
| `CODE_REVIEW_STANDARD.md` | 上線前**怎麼查、誰簽核** |

本檔新增一道位於 G0 之前的閘門：**GD（設計閘門）**。
通過 GD 才能開始寫程式；通過 G0–G2（見 `CODE_REVIEW_STANDARD.md`）才能上線。

## 1. GD 設計閘門：開發前五步

凡是「開發新功能／新系統／新服務／新爬蟲／新 pipeline」的請求，AI 必須依序走完下列步驟，**每一步產出經學生確認後才進下一步**：

### 步驟 1｜需求釐清
- 用自己的話複述需求，列出模糊處與假設，向學生提問直到問題定義清楚。
- 必答三問：這是要解決**什麼問題**？**誰**會用？**不做**會怎樣？

### 步驟 2｜現況調查
- 先搜尋本 repo 與實驗室既有系統是否已有可重用或可擴充的實作
  （本 repo 已有大量爬蟲、RAG、測試腳本——很多「新需求」其實是既有件的參數化）。
- 調查現成套件／服務能否直接解決；「不用寫程式」是合格的結論。
- 產出：可重用清單 + 缺口分析。

### 步驟 3｜設計
- 依 `docs/templates/DESIGN_DOC.md` 撰寫設計文件，**至少比較兩個方案**並說明取捨。
- 存放於 `docs/designs/YYYY-MM-DD-<slug>.md`。

### 步驟 4｜評估
- 依 `docs/templates/PRE_DEV_ASSESSMENT.md` 填寫開發前評估表（風險 Tier、成本、成功指標、回滾方式）。
- Tier 分級沿用 `CODE_REVIEW_STANDARD.md` 第 2 節的 Tier A/B/C 定義。
- **Tier A、Tier B：須取得指導老師核可後才能進入實作。**
- Tier C：學生自行確認評估表即可，但文件仍須存檔。

### 步驟 5｜實作與驗證
- 通過 GD 後才開始寫程式；遵守 `CODING_STYLE.md`，小步提交。
- 完成後依 `CODE_REVIEW_STANDARD.md` 走 G0–G2。
- Commit / PR 描述須引用對應的設計文件路徑。

## 2. AI 硬性行為規則

- **R1** 收到「幫我做一個〇〇系統／功能」類請求時，不得直接產生實作程式碼，一律先進入步驟 1。
- **R2** GD 未通過前，AI 只能輸出：提問清單、調查結果、設計文件、評估表、示意用的介面草稿（型別／API 簽名／schema 草案）。不得輸出可執行的系統實作。
- **R3** 學生要求跳過流程時：AI 應提醒本規範一次，並詢問「指導老師是否已同意豁免」。學生明確回答已同意才可續行，且須在評估表記載豁免事由；否則維持流程。
- **R4** 每個新任務開場，AI 應主動判斷該任務屬於「須走 GD」或「第 3 節例外」，並向學生說明判斷理由。
- **R5** AI 不得替學生填寫評估表的簽核欄位，也不得代答「老師已同意」。
- **R6** 使用 plan mode（計畫模式）時，計畫本身不可包含未經 GD 的完整系統實作步驟；計畫應以 GD 五步為骨架。
- **R7** 回覆一律使用繁體中文；程式碼識別字與註解依 `CODING_STYLE.md`。

## 3. 例外：免走完整 GD 的情形

同時滿足「小、可逆、不對外」才算例外，有疑義時從嚴（走 GD）：

1. **小型修正**：單次變更 ≤ 30 行，且不新增依賴、不變更資料庫 schema、不新增對外介面（API／port／排程）。
2. **純文件與註解**：README、docstring、規範文件的修改。
3. **測試補強**：只新增或修正測試，不動被測程式。
4. **一次性分析腳本**：僅供當下分析、不進 `src/`、不排程、檔頭標註 `# throwaway`。

例外情形仍須遵守 `CODING_STYLE.md`；若後續要「轉正」（進 `src/`、排程、對外），即視為新開發，回頭補走 GD。

## 4. 常見違規樣態（AI 應主動攔下）

- 「先寫個 prototype 看看」→ prototype 也是實作；請先完成步驟 1–2，若確有必要，在評估表中登記為「探索性 spike」並限定範圍與時限。
- 「參考網路上的範例直接改」→ 來源、授權、安全性未評估前不得併入。
- 「先上線再補文件」→ 違反 G2；設計文件是送審包（Review Package）的一部分。
- 「這只是課堂 demo」→ demo 若會部署到共用機器或對外開 port，一律視為 Tier B 以上。

## 5. 待老師拍板事項

- 例外門檻「≤ 30 行」是否合適（可對齊 `CODE_REVIEW_STANDARD.md` R-08 的討論）。
- Tier C 是否也需要老師抽查評估表。
- 探索性 spike 的時限上限（建議：2 個工作天，逾期須補設計文件）。

## 版本紀錄

- v1.0（2026-08-18）初稿，新增 GD 設計閘門與 AI 行為規則，配套 `docs/templates/` 兩份模板。
