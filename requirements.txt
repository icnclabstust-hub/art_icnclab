# 藝術史資料庫 — Python 依賴
#
# 本檔由掃描 repo 內 130 個 .py 檔的 import 產生。
# 未鎖定版本：repo 內沒有保留任何既有環境的 lock/freeze 檔
# （langchain-env / importer_venv / rl_venv / cuda_art_env 皆在 .gitignore 內未追蹤），
# 無從還原原始版本。若日後有可運作環境，請以 pip freeze 覆蓋此檔。

# ==================== 資料庫 / 檢索 ====================
neo4j                    # 圖資料庫驅動（知識圖譜主力）
chromadb                 # 向量資料庫
elasticsearch            # scripts/sync_chromadb_to_elasticsearch.py
redis                    # 快取與 agent 通訊匯流排

# ==================== LangChain / RAG ====================
langchain
langchain-core
langchain-community
langchain-openai
langchain-neo4j

# ==================== 模型 / 嵌入 ====================
sentence-transformers    # BGE-M3 等嵌入模型
transformers
torch                    # CUDA 版請改用 https://download.pytorch.org/whl/cuXXX 索引安裝
faiss-cpu                # 若走 GPU 檢索請改裝 faiss-gpu
numpy
scipy

# ==================== Web 服務 ====================
fastapi                  # RAG API（RAG_API_PORT=8008）與監控服務
uvicorn[standard]
pydantic                 # 亦為 OpenWebUI pipes 的 Valves 定義所需
flask                    # ml-service/
flask-cors
httpx
requests
aiohttp                  # MCP tool proxy / registry
urllib3

# ==================== 爬蟲 / 排程 ====================
schedule                 # src/crawler/AutomatedCrawlerSystem.py

# ==================== 文件處理 ====================
PyPDF2                   # src/importer/pdf_processor.py
pdfplumber
python-docx              # scripts/eval_step*.py 評測流程
openpyxl                 # 評測結果輸出 xlsx
Pillow                   # 多模態 RAG 影像處理
PyYAML

# ==================== 中文處理 ====================
OpenCC                   # 簡繁轉換，11 支 OpenWebUI pipe 皆使用

# ==================== 監控 / 效能 ====================
psutil
prometheus-client
nvidia-ml-py             # 提供 pynvml；GPU 監控
GPUtil
matplotlib               # 策略比較分析圖表
tqdm
