# 實驗室 系統上線前 Code Review 標準 v1.1

> 適用對象：本實驗室所有準備上線（對外或對校內使用者提供服務）的系統與其開發者。
> 制定日期：2026-08-18。狀態：**草案，待蔡老師拍板**。
> 配套文件：《實驗室 Coding Style 規範 v1.0》（`CODING_STYLE.md`）。
> **分工：Style 規範定義「程式怎麼寫」；本文定義「上線前怎麼查、誰簽字、什麼條件才准上線」。**

---

## 0. 為什麼還要這份文件

`CODING_STYLE.md` §0 已拍板「開發後由老師進行程式碼檢視後才能上線」，並在附錄 C 給了 10 項檢核表。但實際要執行時缺三樣東西：

1. **誰在什麼時候查** — 沒有流程節點，實務上會退化成「上線前一晚老師一個人扛」。
2. **查到問題算不算擋** — 沒有嚴重度分級，「這個變數名可以更好」和「初始密碼硬編碼上了 GitHub」用同一個音量講話。
3. **查完留什麼證據** — 沒有簽核紀錄，日後出事無法回溯當初憑什麼放行、誰放行的。

本文補這三項，並把檢核範圍從「程式碼」擴到「上線」真正該看的東西：設定、密鑰、資料邊界、備份、回滾、監控。

### 設計原則（採自業界標準，出處見附錄 F）

- **放行判準不是完美，是「合入後整體 code health 一定變好」**（Google eng-practices）。用追求完美擋住改善，是把 review 變成阻礙。
- **檢視有物理極限**（SmartBear × Cisco 研究：2,500 次 review／320 萬行／50 位開發者）：單次 200–400 行、60–90 分鐘、速度低於 500 行/小時，才有 70–90% 缺陷檢出率。超過這個量就是走過場，不如不做。
- **風險導向，不是一視同仁**（Google SRE Launch Checklist、OWASP 風險導向檢視）：檢視深度隨系統風險調整。小實驗室不可能用同一份最重的清單套所有系統。
- **能自動查的不用人查**（NIST SP 800-218 SSDF PW.7）：工具負責格式、lint、已知 CVE、secrets；人只看工具看不出來的——業務邏輯、授權判斷、競態、架構、資料邊界。
- **基線 vs 差異兩種檢視**（OWASP Secure Code Review Cheat Sheet）：首次上線要看全部（baseline），日常改動只看 diff。
- **程式碼被讀的次數遠多於被寫**（Clean Code, Robert C. Martin）：檢視的終極對象是可讀性與可改性——下一位接手的同學能不能不靠作者在場就看懂、敢改。實驗室人員每屆輪替，這條對我們比對業界更重要。

---

## 1. 三種檢視與適用時機

| 代號 | 名稱 | 觸發時機 | 檢視範圍 | 執行者 |
|------|------|----------|----------|--------|
| **RV-1** | 日常變更檢視（diff review） | 每次功能合入 | 本次 diff | 同儕（非作者） |
| **RV-2** | **上線前基線檢視（baseline review）** | 首次上線／重大改版／換部署環境／換公網出口 | 全系統 | 同儕 + 老師 |
| **RV-3** | 上線後回檢 | 上線後 24 小時、7 天 | 日誌、監控、實際行為 | 作者 |

**本文主體是 RV-2。** RV-1 用附錄 A 的一頁式速查即可，不必動用完整流程。

---

## 2. 系統風險分級（Tier）

先分級，再決定要做多少事。四題自評，符合項目越多，級別越高。

| 判準 | 說明 |
|------|------|
| 公網可達？ | 走 Cloudflare Tunnel、對外網域、或校外可連 |
| 有個資或成績？ | 學號、姓名、聯絡方式、評分結果、諮商／輔導紀錄 |
| 有不可逆動作？ | 刪除、帳號合併、成績重算、對外寄信、金流 |
| 多人同時使用？ | 非單人自用工具 |

| Tier | 定義 | 現有系統對照 | 要做什麼 |
|------|------|--------------|----------|
| **A（高）** | 公網 **且** 有個資／成績 | ai_quiz、ipas_learn、comm_advisor | 全部檢核表 A–G + 老師簽核 + RV-3 回檢 |
| **B（中）** | 公網但無個資；或有個資但僅內網 | art_icnclab RAG 服務、anchor_system、creathink | 表 A/B/C/D/G（含 LLM 者 E 表必做）+ 老師簽核 |
| **C（低）** | 單人工具、內網、無個資、無不可逆動作 | PMS3（依其現行個人工具定位） | 附錄 A 速查 + 機器閘全綠；老師知會，不必簽核 |

- **T-Tier-01 [MUST]** Tier 由作者自評、老師確認。**有疑義時一律往上取。**
- **T-Tier-02 [MUST]** 系統定位改變（例：PMS3 從個人工具改成多人可用）即重新分級，並重跑一次 RV-2。分級是對現況的判斷，不是一次性標籤。

---

## 3. 三道閘門

```
作者 ──► G0 機器閘 ──► G1 同儕檢視 ──► G2 上線前檢視 + 簽核 ──► 上線 ──► RV-3 回檢
        (工具全綠)     (另一位同學)      (老師)                         (24h / 7d)
```

| 閘 | 誰執行 | 查什麼 | 通過條件 | 留下什麼證據 |
|----|--------|--------|----------|--------------|
| **G0** | 作者 | `ruff check`／`ruff format --check`／`gitleaks`／`pip-audit`／`pytest` | 五項全綠（無測試者須說明理由並列為 S2） | 終端輸出貼入送審包 |
| **G1** | 同儕 | 表 A（通用品質）+ 表 G（Clean Code）+ 表 B（安全）重點項 | 無 S0／S1 未關閉項 | 檢視紀錄檔（附錄 C 模板） |
| **G2** | 老師 | 表 C/D/E/F + G1 未關閉項 + 送審包完整性 | 簽核表所有欄位完成 | 簽核表（§8）存檔 |

- **R-01 [MUST]** 未過 G0 不得送 G1。送審時**附工具輸出**，不是口頭說「有跑過」。
- **R-02 [MUST]** G1 的 reviewer 不得是作者本人。若當下確無第二人，由老師併入 G2 執行，並在簽核表註記「無同儕檢視」——這是已知的檢出率損失，要被記錄，不是被默認。
- **R-03 [MUST]** Tier A／B 未完成 G2 簽核，不得對外開放（**包含開通 tunnel、發布網址、給學生試用**）。
- **R-04 [SHOULD]** G0 → G2 全程 ≤ 5 個工作天。卡關超過即上報；不要讓上線日倒逼檢視品質。

---

## 4. 送審包（Review Package）

作者交出以下七項。**缺件退回，不進入檢視**——這是保護 reviewer 的時間，也是逼作者先自己想過一遍。

| # | 項目 | 具體要求 |
|---|------|----------|
| 1 | 系統一頁說明 | 用途、使用者是誰、資料流（誰的資料進來、存到哪、誰看得到）、對外端點清單、Tier 自評與理由 |
| 2 | 部署拓樸 | 跑在哪台機器、哪個 port、經不經 tunnel、依賴哪些服務（SQLite／Neo4j／ChromaDB／Ollama／Open WebUI）與其版本 |
| 3 | 機器閘輸出 | `ruff check .`、`gitleaks detect`、`pip-audit`、`pytest` 四份終端輸出 |
| 4 | 版控邊界證據 | `git ls-files` 完整輸出（驗證 S-18／S-19，用指令不用肉眼） |
| 5 | `.env.example` | 變數名齊全、附說明、**無任何真值** |
| 6 | 回滾方案 | 怎麼停、怎麼退版、資料怎麼還原 |
| 7 | 檢視範圍與規模 | RV-2 需附檔案清單與行數統計，用來排檢視場次 |

- **R-05 [MUST]** 第 6 項回滾方案不得寫「重新 clone」或「再跑一次部署腳本」。必須具體到：停服務的指令、退版的 commit／tag、備份檔的實際路徑與還原指令。**沒有驗證過的回滾方案等於沒有回滾方案**——Tier A 系統須在上線前實際演練一次還原。
- **R-06 [SHOULD]** 第 1 項的資料流用文字或簡圖說明即可，重點是把**信任邊界**畫出來：哪一段的輸入是不可信的。

---

## 5. 檢視執行規則

- **R-07 [MUST]** 單場檢視 **≤ 400 行、≤ 90 分鐘**。超過的系統拆多場（依模組切），場次之間休息。
- **R-08 [MUST]** 檢視速度**不得快於 500 行／小時**。
  > 這條的意思要講白：一套 8,000 行的系統做 RV-2，就是約 16 小時、分 8–10 場。這是規模的代價，不是可以壓縮的行政流程。**真的來不及，就縮上線範圍（先上核心模組、其餘關閉），不要縮檢視**——「全部快速掃一遍」的檢出率遠低於「一半仔細看」。
- **R-09 [SHOULD]** 檢視前先讀送審包第 1、2 項，帶著威脅模型看程式：先理解架構與信任邊界，再看進入點（路由、CLI 參數、爬蟲抓進來的內容）、再追資料流，最後才逐檔看細節。不要一開始就從第一個檔案的第一行讀起。
- **R-10 [MUST]** 意見一律用 **Conventional Comments** 格式，讓「必改」與「順口提」一眼可分：

  ```
  <label> [decoration]: <一句話結論>

  <說明：為什麼、建議怎麼改、對映哪條規範>
  ```

  | label | 用途 |
  |-------|------|
  | `issue` | 明確的問題 |
  | `todo` | 小但必須做的修改 |
  | `suggestion` | 建議的改法（要寫出為什麼更好） |
  | `question` | 不確定是不是問題，需要作者說明 |
  | `nitpick` | 偏好性的小事 |
  | `praise` | 做得好的地方 |
  | `note` | 作者該知道但不必改的事 |

  decoration：`(blocking)`／`(non-blocking)`／`(if-minor)`

  範例：

  ```
  issue (blocking): seed.py:44 硬編碼初始密碼，違反 S-01／S-04。

  改用 secrets.token_urlsafe(12) 產生一次性密碼，並設 must_change_pw=True。
  這個檔案已推上 GitHub，密碼視為已外洩，請一併輪換現有帳號密碼。
  ```

  ```
  nitpick (non-blocking): 變數 t 建議改叫 timeout_seconds（N-04）。
  ```

- **R-11 [MUST]** 每次檢視至少留一則 `praise`。這不是社交禮貌，是讓「哪些做法是對的」被明確記下來，成為下一位同學的樣本——creathink 的 `dummy_verify` 註解就是這樣被寫進 Style 規範 D-02 的。
- **R-12 [SHOULD]** 挑毛病是職責不是失禮，對事不對人（沿用 P-03）。爭議在檢視中收斂不了時**不要讓變更卡死**：當面談 → 找第三人 → 老師裁決，並把結論寫回檢視紀錄，讓下次不必重吵。

---

## 6. 嚴重度分級與放行判準

| 等級 | 定義 | 對映 | 處置 |
|------|------|------|------|
| **S0 阻斷** | 已造成或必然造成外洩、資料損毀、他人可越權 | 違反 [MUST] 且屬 OWASP A01／A04／A07 | 修好並**重新驗證**才放行；已上版控的密鑰須輪換 |
| **S1 重大** | 條件成立即出事；或違反任一 [MUST] 條文 | [MUST] | 上線前修好，不接受「先上再說」 |
| **S2 一般** | 品質／可維護性問題，不影響安全與正確性 | [SHOULD] | 可帶著上線，但須登記負責人與期限（≤ 2 週） |
| **S3 建議** | 風格偏好、命名、可讀性 | nit | 作者自行斟酌，不擋上線 |

- **R-13 [MUST]** S0／S1 **全數關閉**才可簽核。
- **R-14 [MUST]** 帶上線的 S2 逐條登記在簽核表「已知債務」欄，含負責人與到期日。**無人認領的 S2 自動升為 S1**——沒有負責人的技術債不是債，是黑洞。
- **R-15 [MUST]** 密鑰一旦進過 git 歷史（即使當前版本已改掉），一律視為**已外洩**：**輪換該金鑰／密碼**，而不只是刪掉程式碼裡的字串。`git log -S` 查得到的東西，別人也查得到。
- **R-16 [SHOULD]** 放行判準是「上線後整體比現在更安全、更好維護」，不是「零缺陷」。不要用 S3 擋住已把 S0 修好的變更。

---

## 7. 檢核表

> 使用方式：每項標記 ✅ 通過／⚠️ 有發現（填嚴重度）／N/A（填理由）。**N/A 必須寫理由**，空白視同未檢視。
> 「對映」欄指向 `CODING_STYLE.md` 的條文編號，細節去那裡查，本表只列檢查動作。

### 表 A｜通用程式品質（採 Google eng-practices 檢視面向）

| # | 檢查點 | 怎麼查 | 對映 |
|---|--------|--------|------|
| A-01 | **設計** — 這個改動放對地方了嗎？與現有架構相容嗎？ | 看模組邊界；新功能是否該進共用層而不是複製一份 | F-06、G-03 |
| A-02 | **功能** — 真的做到作者宣稱的事嗎？邊界條件呢？ | 至少親手跑一次主要流程；空輸入、超長輸入、並發 | S-08、S-09 |
| A-03 | **過度設計** — 有沒有為「將來可能需要」而寫的功能？ | 找沒有被呼叫的參數、只有一個實作的抽象層 | — |
| A-04 | **測試** — 影響成績／不可逆資料的邏輯有測試嗎？測試會真的失敗嗎？ | 故意改壞一行，看測試是否轉紅 | T-04、T-05 |
| A-05 | **命名** — 名稱表達意圖與單位嗎？ | `timeout_seconds` 而非 `t` | N-01、N-04 |
| A-06 | **註解** — 寫的是「為什麼」而不是「做什麼」？前提假設寫在會被讀到的地方嗎？ | 檢查啟動腳本裡的關鍵假設有沒有進到程式碼 | D-01、D-02、F-05 |
| A-07 | **一致性** — 跟同一專案／同源骨架的其他系統一致嗎？ | 骨架複製系統特別查「漏抄」（見 F-02 反例） | G-03、N-02 |
| A-08 | **文件同步** — README／`.env.example`／部署說明跟這次改動同步了嗎？ | 對照端點清單與 README | — |
| A-09 | **每一行都看過** — 有沒有整片跳過的檔案？ | 對照送審包第 7 項的檔案清單逐一勾選 | — |
| A-10 | **編碼** — 檔案 I/O 有顯式 UTF-8 嗎？ | grep `open(` 與 PowerShell 輸出重導向 | D-04 |

### 表 B｜安全（對映 OWASP Top 10:2025）

> 注意：`CODING_STYLE.md` 附錄 B 目前對映 **2021 版**編號。2025 版已改版（新增 A03 供應鏈、A10 例外處理；A02 設定錯誤升到第 2）。本表用 2025 版，Style 規範改版時請同步（見附錄 E 對照）。

| # | OWASP 2025 | 檢查點 | 對映 |
|---|-----------|--------|------|
| B-01 | **A01 存取控制失效** | 每個端點都問一次「沒登入的人打得到嗎？打到了會怎樣？」健康檢查端點也算 | S-13、S-14、F-02 |
| B-02 | A01 | 有沒有「改 URL 上的 id 就看得到別人資料」（IDOR）？授權判斷在**伺服器端**做嗎？ | S-13 |
| B-03 | **A02 安全設定錯誤** | 生產啟動無 `--reload`、無 debug、`docs_url=None` | F-01 |
| B-04 | A02 | 公網服務掛 `TrustedHostMiddleware` 白名單 | F-02 |
| B-05 | A02 | CORS：同源就不掛；要掛則明列來源，禁 `["*"]` | F-03 |
| B-06 | A02 | 預設值是安全的嗎？（預設關閉 > 預設開放） | — |
| B-07 | **A03 軟體供應鏈失效** | `requirements.txt` 全數 `==` 釘版、檔頭有釘版日期 | S-16 |
| B-08 | A03 | `pip-audit` 通過；Node 專案 `npm audit`；**有未處理 CVE 不得上線** | S-17 |
| B-09 | A03 | 新增依賴是誰維護的？最後更新是什麼時候？值得為這個功能引入嗎？ | — |
| B-10 | **A04 密碼學失敗** | 密碼雜湊 PBKDF2-HMAC-SHA256 ≥200,000 迭代／bcrypt／argon2；每人獨立 salt | S-10 |
| B-11 | A04 | 傳輸加密：對外一律 HTTPS（tunnel 已提供則確認沒有旁路的明文 port） | F-02 |
| B-12 | **A05 注入** | SQL 全參數化；Cypher／向量庫查詢不用字串拼接；動態表名只能來自程式內常數 | S-05、S-06 |
| B-13 | A05 | shell 呼叫有沒有把外部輸入拼進命令列？ | — |
| B-14 | **A06 不安全設計** | 初始／重設密碼不可預測，強制首登改密碼 | S-04 |
| B-15 | A06 | 節流的信任前提成立嗎？（IP 判定是否可被偽造） | F-04、F-05 |
| B-16 | **A07 身分驗證失敗** | 憑證比較用 `hmac.compare_digest`；失敗節流；查無帳號跑 `dummy_verify` 防列舉 | S-11 |
| B-17 | A07 | Session cookie `httpOnly`、逾時 ≤ 8 小時、token 用 `secrets.token_urlsafe(32)` | S-12 |
| B-18 | **A08 軟體與資料完整性失效** | `gitleaks` 通過；`.env` 已 ignore；下載的模型／資料有來源與校驗 | S-02、T-02 |
| B-19 | **A09 日誌與告警失效** | log 不含密碼、token、個資原文；log 檔已 ignore | S-03、S-15 |
| B-20 | A09 | 出事時查得到嗎？失敗登入、異常請求有沒有留下可追蹤的紀錄？ | S-15 |
| B-21 | **A10 例外處理不當**（2025 新增） | `except` 精準捕捉；`detail=str(exc)` 只用於自訂領域例外 | S-14 |
| B-22 | A10 | 轉型失敗不得炸 500（`?min=abc`）；外部服務逾時／掛掉時的行為是什麼？ | S-09 |
| B-23 | A10 | 失敗時是「安全地失敗」還是「開放地失敗」？（驗證服務掛掉 → 應拒絕，不是放行） | — |
| B-24 | 密鑰總查 | 全 repo grep 一次密碼／key／token；含 README、seed 腳本、測試、notebook | S-01 |

### 表 C｜上線部署與運維（採 Google SRE Launch Checklist 精神）

| # | 檢查點 | 判準 |
|---|--------|------|
| C-01 | 服務怎麼啟動、怎麼停？ | 有明確指令／服務單元，不是「開個終端跑著」 |
| C-02 | 機器重開後會自己起來嗎？ | 排程／服務註冊已設定並實測 |
| C-03 | 依賴服務掛掉時的行為？ | Neo4j／Ollama／DB 不可用時回明確錯誤，不是 500 堆疊 |
| C-04 | 健康檢查端點 | 只回狀態字（`ok`／`degraded`），不洩露路徑或例外原文（S-14） |
| C-05 | 資料備份 | 備份在哪、多久一次、**還原演練過了嗎** |
| C-06 | 回滾方案 | 送審包第 6 項已驗證（R-05） |
| C-07 | 資源上限 | 有沒有可能被一個請求打爆？（大檔上傳、無上限查詢、無分頁） |
| C-08 | 公網出口設定 | tunnel 設定正確（本實驗室已知：`protocol: http2`，QUIC 會壞） |
| C-09 | 埠曝露面 | `netstat` 確認只有該開的 port 對外；服務埠是否被防火牆限制為僅接受 tunnel／localhost |
| C-10 | 監控與告警 | 掛了誰會知道？多久會知道？ |
| C-11 | 環境隔離 | 測試不會寫到正式資料（沿用 P-02：先在拋棄式資料驗證） |
| C-12 | 上線窗口 | 避開課程／考試等關鍵時段；有時效窗的功能先在臨時資料驗證 |

### 表 D｜資料與隱私、版控邊界

| # | 檢查點 | 怎麼查 | 對映 |
|---|--------|--------|------|
| D-01 | DB 檔（含 `-wal`／`-shm`）、`data/`、`*.pem`、log 檔全數未被追蹤 | `git ls-files`，不用肉眼 | S-18 |
| D-02 | **被追蹤的檔案**不含真實人名、可識別個資 | 含測試檔、prompt、詞表、README、notebook 輸出 | S-19 |
| D-03 | 蒐集的個資是「必要」的嗎？ | 沒有用到的欄位就不要收 | — |
| D-04 | 個資保存期限與刪除機制 | 學期結束後怎麼處理 | — |
| D-05 | 第三方資料的授權 | 爬蟲抓來的圖檔／文本，授權條款允許這樣用嗎？（Europeana、Harvard、AIC、Cleveland 各有不同條款） | — |
| D-06 | notebook 輸出 | `.ipynb` 的 cell output 常夾帶資料樣本與路徑，提交前清空 | S-19 |

### 表 E｜LLM／RAG 專章（對映 OWASP Top 10 for LLM Applications 2025）

> Tier A／B 中含 LLM 或 RAG 的系統必做（art_icnclab、comm_advisor、ai_quiz 出題功能等）。
> 這是 `CODING_STYLE.md` v1.0 **未涵蓋**的區塊，也是實驗室目前最大的規範缺口。

| # | LLM Top 10 | 檢查點 |
|---|-----------|--------|
| E-01 | **LLM01 提示詞注入** | 使用者輸入有沒有被直接拼進 system prompt？**間接注入**：爬蟲抓回的網頁／館藏描述進入 context 後，其中的「忽略上述指令」會被照做嗎？ |
| E-02 | LLM01 | 檢索到的文件內容在送入模型前，有沒有明確標記為「資料」而非「指令」？ |
| E-03 | **LLM02 敏感資訊洩漏** | 模型回應會不會帶出其他使用者的資料、內部路徑、API key？RAG 檢索範圍有做使用者隔離嗎？ |
| E-04 | **LLM03 供應鏈** | 模型／embedding 權重來源可信嗎？版本釘住了嗎？（`download-models.sh` 的來源與校驗） |
| E-05 | **LLM04 資料與模型污染** | 進入向量庫的內容有來源標記嗎？誰可以寫入向量庫？爬蟲抓到的內容能不能被污染？ |
| E-06 | **LLM05 輸出處理不當** | 模型輸出有沒有被直接當成 HTML 渲染（XSS）、當成 SQL／Cypher 執行、或當成 shell 指令？ |
| E-07 | **LLM06 過度代理權** | agent 能碰到哪些工具？有沒有給到不必要的寫入／刪除權限？（`agents/` 目錄逐一檢查） |
| E-08 | **LLM07 系統提示詞洩漏** | system prompt 裡有沒有放密鑰或不該被知道的規則？假設它一定會被問出來 |
| E-09 | **LLM08 向量與嵌入弱點** | 向量庫的存取控制；embedding 是否可反推原文；跨使用者檢索隔離 |
| E-10 | **LLM09 錯誤資訊** | 回應有沒有標示來源？幻覺的處置是什麼？學術用途的系統尤其要能溯源（本專案已有 source metadata，確認有暴露到回應層） |
| E-11 | **LLM10 無節制消耗** | 單次請求的 token 上限、每人速率上限、逾時設定；本地 Ollama 被打爆時整台機器的其他服務會不會一起倒 |

### 表 F｜Node.js 爬蟲與前端補充

> 本實驗室 art_icnclab 為 Node 爬蟲 + Python RAG 混合架構，此表補 Style 規範以 Python 為主的缺口。

| # | 檢查點 |
|---|--------|
| F-01 | `package.json` 依賴釘版；`package-lock.json` 已提交；`npm audit` 通過 |
| F-02 | 爬蟲遵守目標站 `robots.txt` 與 API 使用條款；有速率限制與 User-Agent 標示 |
| F-03 | 爬蟲失敗的處理：重試上限、逾時、斷點續爬，不會無限重試打爆對方 |
| F-04 | 抓回的內容在寫入前有清洗／驗證（連到表 E 的 LLM04） |
| F-05 | 前端渲染的資料有做輸出編碼（連到表 E 的 LLM05） |
| F-06 | `node_modules/`、`venv/`、`__pycache__/`、`.ruff_cache/` 全數未被追蹤 |

### 表 G｜Clean Code（函式與結構層，採 Robert C. Martin《Clean Code》）

> 命名與註解已在表 A（A-05、A-06），本表專看**函式、類別、結構**這一層——工具查不出來、但決定三年後這套系統還改不改得動的東西。
> 判定原則：這裡的行數／層數門檻是**觸發討論的訊號**，不是機械紅線；超標但有好理由（且理由寫在註解裡）可以過。

| # | 原則 | 檢查點 | 對映 |
|---|------|--------|------|
| G-01 | **函式小而專一** | 一個函式只做一件事、停留在同一個抽象層次。超過約 50 行、或需要滾動才能看完整個函式，即問「能不能拆」 | F-06（>500 行拆檔的函式版） |
| G-02 | **參數數量** | ≤ 3 個為原則；更多時改傳 dataclass／Pydantic model，或用 keyword-only 參數。布林旗標參數（`do_x(data, True)`）是「這函式在做兩件事」的訊號 | — |
| G-03 | **無副作用驚喜** | 函式名沒說的事不要做：`check_password()` 不該順便初始化 session。查詢與修改分離——回傳答案的函式不改狀態 | — |
| G-04 | **DRY** | 同一段邏輯第三次出現即抽共用。骨架複製系統（ipas_learn／ai_quiz 一族）之間的複製要**有意識**：複製時知道自己在複製什麼，修 bug 時知道要修幾份 | A-07、G-03(Style) |
| G-05 | **魔術數字／字串** | 裸數字與裸字串提為具名常數，名稱含單位與依據（`SESSION_TIMEOUT_HOURS = 8  # S-12`）。出現兩次以上的字串 key 同理 | N-04 |
| G-06 | **死碼** | 註解掉的程式碼、沒人呼叫的函式、永遠走不到的分支，一律刪除——git 歷史會記得，留著只會誤導下一位讀者 | A-03 |
| G-07 | **巢狀深度** | 用 guard clause／early return 取代深層 if；巢狀超過 3 層即重構。錯誤情況先排除、提早離開，主流程留在最外層直行 | — |
| G-08 | **錯誤處理與主邏輯分離** | 用例外，不用錯誤碼回傳值；不回傳 `None` 當隱形錯誤狀態（要就明確標 `Optional` 型別並在呼叫端就地處理）。try 區塊裡只放會失敗的那幾行 | S-14、D-03 |
| G-09 | **類別單一職責（SRP）** | 一個類別只有一個變動理由。一個什麼都管的 god class／god module 是拆分訊號——對映單一 `main.py` 超過約 500 行即拆的既有規則 | F-06 |
| G-10 | **不挖穿結構（Law of Demeter）** | 避免 `a.b.c.d` 一路挖穿別人的內部；跟「直接朋友」說話。挖穿代表呼叫端知道太多內部結構，對方一改就全斷 | — |
| G-11 | **測試也是程式碼（FIRST）** | 測試要快（Fast）、彼此獨立（Independent）、可重複（Repeatable，不依賴外部服務與執行順序）、自我驗證（Self-validating，不靠人眼看輸出）、及時（Timely）。**測試碼適用和產品碼相同的整潔標準**——爛測試比沒測試更拖累重構 | T-04、T-05 |
| G-12 | **童軍規則** | 離開時比來時乾淨：碰到的程式碼順手做一項小改善（改個名、抽個常數、刪段死碼）。**但範圍紀律優先**：清理僅限本次觸碰範圍，不重排無關程式碼、不把重構混進功能 commit | G-03(Style)、P-01 |

**與嚴重度分級的關係**：表 G 的發現預設為 **S2（登記負責人與期限）或 S3（作者斟酌）**，不擋上線——除非已實際造成正確性問題（例：G-03 的隱藏副作用導致資料被意外改寫），才升 S1。這符合放行判準 R-16：Clean Code 是「持續變好」的方向，不是上線門檻的門神。

---

## 8. 簽核表（存檔格式）

每次 RV-2 產生一份，檔名 `review/RV2-<系統名>-<YYYYMMDD>.md`，隨 repo 版控。

```markdown
# 上線前檢視簽核表

系統名稱：
版本／commit：
Tier 分級：        （自評 / 老師確認）
送審日期：         上線預定日期：

## 一、送審包完整性（老師勾選）
- [ ] 1 系統一頁說明   - [ ] 2 部署拓樸   - [ ] 3 機器閘輸出
- [ ] 4 git ls-files   - [ ] 5 .env.example   - [ ] 6 回滾方案（已演練）
- [ ] 7 檢視範圍與行數

## 二、機器閘 G0
| 工具 | 結果 | 備註 |
|------|------|------|
| ruff check | | |
| ruff format --check | | |
| gitleaks | | |
| pip-audit / npm audit | | |
| pytest | | |

## 三、同儕檢視 G1
reviewer：            日期：            檢視行數：       場次數：
（無同儕檢視時填「無」並說明原因）

## 四、檢核表結果
| 表 | 通過 | 有發現 | N/A | 說明 |
|----|------|--------|-----|------|
| A 通用品質 | | | | |
| B 安全 | | | | |
| C 部署運維 | | | | |
| D 資料隱私 | | | | |
| E LLM/RAG | | | | |
| F Node/前端 | | | | |
| G Clean Code | | | | |

## 五、發現事項
| 編號 | 位置 | 嚴重度 | 描述 | 對映條文 | 狀態 |
|------|------|--------|------|----------|------|
| 1 | | S0/S1/S2/S3 | | | 已修/待修 |

## 六、已知債務（帶上線的 S2）
| 編號 | 描述 | 負責人 | 到期日 |
|------|------|--------|--------|

## 七、簽核
- S0／S1 全數關閉：□ 是  □ 否（否則不得簽核）
- 同意上線：□ 是  □ 有條件同意（條件：______）  □ 否
- 檢視人簽名／日期：
- 放行人（老師）簽名／日期：
```

---

## 9. 上線後回檢（RV-3）

- **R-17 [MUST]** Tier A／B 系統上線後 **24 小時**內看一次：錯誤日誌、失敗登入紀錄、資源用量、是否有非預期來源的請求。
- **R-18 [MUST]** 上線後 **7 天**內看一次：已知債務（S2）是否照期程處理、有沒有新出現的錯誤樣態。
- **R-19 [SHOULD]** RV-3 的發現寫回簽核表末尾，形成閉環。真實踩坑要回饋進 `CODING_STYLE.md`——實驗室規範的價值就在於**每一條都有反例出處**，不是抄來的清單。

---

## 附錄 A｜一頁式速查（RV-1 日常檢視用，可貼進 PR 描述）

```
[ ]  1. 密鑰／密碼只存在 .env？（全 repo grep 一次，含 README 與測試）
[ ]  2. 新增的 SQL／Cypher 全參數化，無字串拼接？
[ ]  3. 對外輸入有 Pydantic schema 或白名單驗證？轉型失敗不炸 500？
[ ]  4. except 精準捕捉？str(exc) 只包自訂例外？
[ ]  5. 新增檔案 I/O 顯式 UTF-8？
[ ]  6. 新增依賴已釘版、過 pip-audit / npm audit？
[ ]  7. 公開函式有 Google docstring？註解寫「為什麼」？
[ ]  8. 影響成績／不可逆資料的改動附測試？測試會真的失敗？
[ ]  9. log 沒多印不該印的（密碼、token、個資）？
[ ] 10. git status / git ls-files：沒有不該追蹤的檔案？
[ ] 11. 生產設定沒有 --reload、docs_url 已關？
[ ] 12. 公網服務掛 TrustedHost？CORS 沒開 *？
[ ] 13. 新端點問過「沒登入的人打得到嗎」？
[ ] 14. （含 LLM）使用者輸入沒被直接拼進 system prompt？
[ ] 15. （含 LLM）檢索內容標記為資料而非指令？模型輸出沒被直接執行／渲染？
[ ] 16. 有沒有為「將來可能需要」而寫的功能？
[ ] 17. 這次改動有沒有讓 README／.env.example 過期？
[ ] 18. 新函式只做一件事？參數 ≤ 3？布林旗標參數是拆分訊號。
[ ] 19. 沒有留下註解掉的程式碼與死碼？（git 會記得，直接刪）
[ ] 20. 裸數字／字串提成具名常數（含單位）？巢狀 > 3 層用 early return 拆平？
[ ] 21. 單次檢視 ≤ 400 行、≤ 90 分鐘？超過就拆場。
[ ] 22. 意見用 Conventional Comments 標了 blocking / non-blocking？
[ ] 23. 留了至少一則 praise？
```

## 附錄 B｜機器閘指令（G0）

Python 專案：

```bash
ruff check . && ruff format --check . && gitleaks detect --no-banner && pip-audit && pytest -q
```

Node 專案：

```bash
npm audit --audit-level=high && npm test
```

版控邊界與規模統計：

```bash
git ls-files > review/tracked-files.txt && git ls-files "*.py" "*.js" | xargs wc -l | tail -1
```

密鑰總查（表 B-24）：

```bash
grep -rniE "(password|passwd|secret|api[_-]?key|token)[[:space:]]*[=:]" --include="*.py" --include="*.js" --include="*.md" --include="*.yaml" .
```

## 附錄 C｜檢視紀錄檔模板（G1 用）

```markdown
# 檢視紀錄
系統／範圍：            commit：
reviewer：              日期：
本場檢視行數：          耗時：      （R-07：≤400 行 / ≤90 分鐘）

## 意見
issue (blocking): <檔案:行> <一句話結論>
  <說明與建議改法，附對映條文>

suggestion (non-blocking): ...
nitpick (non-blocking): ...
praise: ...

## 結論
□ 通過   □ 修正後通過   □ 退回
未關閉 S0/S1 數量：
```

## 附錄 D｜與 `CODING_STYLE.md` 的分工

| 問題 | 去哪查 |
|------|--------|
| 這行程式該怎麼寫？命名／格式／docstring | `CODING_STYLE.md` §2–§5 |
| 這條規則的反例是什麼？ | `CODING_STYLE.md`（每條附實際踩坑出處） |
| 工具怎麼裝、設定檔長怎樣？ | `CODING_STYLE.md` 附錄 A |
| 上線前要查什麼、誰簽字？ | **本文** §7、§8 |
| 查到問題算不算擋？ | **本文** §6 |
| 檢視要花多久、怎麼排場次？ | **本文** §5 |
| LLM／RAG 系統要查什麼？ | **本文** 表 E（Style 規範未涵蓋） |
| 函式／類別／結構層的可讀性怎麼查？ | **本文** 表 G（Clean Code） |

## 附錄 E｜OWASP Top 10 版本對照（2021 → 2025）

`CODING_STYLE.md` 附錄 B 使用 2021 版編號，建議於 v1.1 同步更新為下表。

| 2025 | 名稱 | 2021 對應 | 本文檢核 | Style 條文 |
|------|------|-----------|----------|-----------|
| A01 | 存取控制失效（含 SSRF） | A01 + A10 | B-01、B-02 | S-13、F-02 |
| A02 | 安全設定錯誤 | A05（由第 5 升第 2） | B-03～B-06 | F-01、F-02、F-03、S-02 |
| A03 | **軟體供應鏈失效**（擴大自「易受攻擊元件」） | A06 | B-07～B-09 | S-16、S-17 |
| A04 | 密碼學失敗 | A02 | B-10、B-11 | S-10、S-12 |
| A05 | 注入 | A03 | B-12、B-13 | S-05、S-06、S-07 |
| A06 | 不安全設計 | A04 | B-14、B-15 | S-04、S-11、F-04、F-05 |
| A07 | 身分驗證失敗 | A07 | B-16、B-17 | S-04、S-10、S-11 |
| A08 | 軟體與資料完整性失效 | A08 | B-18 | T-02、S-16 |
| A09 | 日誌與**告警**失效 | A09 | B-19、B-20 | S-03、S-15 |
| A10 | **例外條件處理不當**（新增） | — | B-21～B-23 | S-14、S-09 |

主要變動：SSRF 併入 A01；「易受攻擊與過時元件」擴大為「軟體供應鏈失效」並升至 A03；新增 A10「例外條件處理不當」。**A10 是本實驗室已有實際反例的類別**（creathink `/healthz` 回例外原文、anchor_system 裸 `int()`），對映條文早已存在，只是原本掛在不同編號下。

## 附錄 F｜參考來源

**檢視流程與判準**
- Google Engineering Practices — The Standard of Code Review：https://google.github.io/eng-practices/review/reviewer/standard.html
- Google Engineering Practices — What to Look For in a Code Review：https://google.github.io/eng-practices/review/reviewer/looking-for.html
- SmartBear × Cisco 大規模 code review 研究（200–400 行／60–90 分鐘／<500 LOC/hr）：https://smartbear.com/learn/code-review/best-practices-for-peer-code-review/
- Cisco 案例研究原文：https://static0.smartbear.co/support/media/resources/cc/book/code-review-cisco-case-study.pdf
- Conventional Comments（意見標註格式）：https://conventionalcomments.org/

**程式品質（表 G）**
- Robert C. Martin,《Clean Code: A Handbook of Agile Software Craftsmanship》(Prentice Hall, 2008) — 表 G 各原則出處
- Clean Code 重點摘要（社群整理，速查用）：https://gist.github.com/wojteklu/73c6914cc446146b8b533c0988cf8d29

**安全檢視**
- OWASP Secure Code Review Cheat Sheet（baseline vs diff、風險導向方法）：https://cheatsheetseries.owasp.org/cheatsheets/Secure_Code_Review_Cheat_Sheet.html
- OWASP Top 10:2025：https://owasp.org/Top10/2025/
- OWASP ASVS 5.0：https://owasp.org/www-project-application-security-verification-standard/
- OWASP Top 10 for LLM Applications 2025：https://genai.owasp.org/llm-top-10/
- NIST SP 800-218 SSDF v1.1（PW.7 人工檢視與工具分析）：https://csrc.nist.gov/pubs/sp/800/218/final

**上線準備**
- Google SRE Book — Launch Checklist：https://sre.google/sre-book/launch-checklist/
- Google SRE Book — Reliable Product Launches：https://sre.google/sre-book/reliable-product-launches/

**實驗室內部**
- 《實驗室 Coding Style 規範 v1.0》 `CODING_STYLE.md`

---

## 版本紀錄

| 版本 | 日期 | 變更 |
|------|------|------|
| v1.0 草案 | 2026-08-18 | 初版：以 Google eng-practices／SmartBear-Cisco 研究／OWASP Secure Code Review／OWASP Top 10:2025／OWASP LLM Top 10:2025／NIST SSDF／Google SRE Launch Checklist 為基底，接合實驗室既有 Coding Style 規範條文編號 |
| v1.1 草案 | 2026-08-18 | 新增表 G「Clean Code」（函式與結構層 12 項，納入 G1 同儕檢視）；速查表增列三項（18–20）；設計原則補「程式碼被讀多於被寫」 |

### 待老師拍板事項

1. Tier 分級中六套既有系統的歸屬是否同意（尤其 PMS3 是否維持 Tier C）。
2. R-08 檢視速度上限是否照收——這條決定 RV-2 的實際工時，是本文最貴的一條。
3. 是否要求既有六套系統補做 RV-2，或僅適用於新上線系統與重大改版。
4. `CODING_STYLE.md` 附錄 B 是否依附錄 E 更新為 OWASP 2025 編號（連帶 v1.1 改版）。
